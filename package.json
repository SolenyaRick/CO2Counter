{
  "name": "co2-tracker",
  "version": "1.0.0",
  "description": "CO2 Tracker - static web app wrapped for iOS via Capacitor",
  "private": true,
  "scripts": {
    "build:web": "sh scripts/sync-web.sh",
    "sync:ios": "npm run build:web && cap sync ios",
    "open:ios": "npm run sync:ios && cap open ios"
  },
  "dependencies": {
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.4.2",
    "@capacitor/ios": "^8.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2"
  }
}
